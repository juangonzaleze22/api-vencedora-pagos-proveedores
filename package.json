{
  "name": "api-vencedora-pagos-proveedores",
  "version": "1.0.0",
  "description": "API para gestión de pagos a proveedores",
  "main": "dist/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "nodemon",
    "build": "node -e \"try { require('fs').rmSync('dist', { recursive: true }) } catch (e) {}\" && prisma generate && prisma db push && tsc",
    "build:deploy": "node -e \"try { require('fs').rmSync('dist', { recursive: true }) } catch (e) {}\" && prisma generate && tsc",
    "build:hostinger": "npm run build:deploy && bestzip deploy.zip package.json package-lock.json dist prisma",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:dbpush": "prisma db push",
    "prisma:migrate": "prisma migrate dev",
    "prisma:seed": "ts-node prisma/seed.ts",
    "prisma:studio": "prisma studio",
    "update-debt-statuses": "ts-node src/scripts/update-debt-statuses.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pdfkit": "^0.17.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.10.5",
    "@types/pdfkit": "^0.17.5",
    "bestzip": "^2.2.1",
    "nodemon": "^3.0.2",
    "prisma": "^5.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
